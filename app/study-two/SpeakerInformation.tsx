import type {ParticipationSnapshot} from '@/lib/study-two/participation';
export default function SpeakerInformation({information}:{information:ParticipationSnapshot}){
 return <>{information.sections.map(section=><section className="s1-information" key={section.title}><h2>{section.title}</h2><p>{section.text}</p></section>)}<section className="s1-information s2-review-notice"><h2>Participant information is under review</h2><p>This walkthrough is for testing with invented answers. Research participation is not open. Before anyone is invited, the study team must confirm:</p><ul>{Object.entries(information.pending).map(([key,value])=><li key={key}>{({eligibility:'Who is eligible',researchContact:'Who to contact with questions',retention:'How long responses and identifying details are kept',withdrawal:'How to withdraw saved responses',recording:'Panel recording permissions and use',debrief:'The final study explanation'} as Record<string,string>)[key]}: {value??'to be confirmed'}.</li>)}</ul><p>The test acknowledgement is not research consent.</p></section></>;
}
