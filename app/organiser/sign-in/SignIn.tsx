'use client';
import {useState,type FormEvent} from 'react';
export default function SignIn(){const [busy,setBusy]=useState(false),[error,setError]=useState('');async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const form=e.currentTarget;try{const response=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:new FormData(form).get('password')}),cache:'no-store'});if(!response.ok)throw new Error(response.status===401?'That password was not accepted.':'Sign-in is unavailable. Try again shortly.');form.reset();const next=new URLSearchParams(location.search).get('next')??'/results';location.assign(/^\/(results|administration|study-guides|study-two\/(results|review)|admin)(\/|$|\?)/.test(next)&&!next.includes('\\')?next:'/results');}catch(e){setError((e as Error).message);}finally{setBusy(false);}}return <form onSubmit={login} className="workspace-login"><label htmlFor="workspace-password">Study admin password</label><input id="workspace-password" name="password" type="password" autoComplete="current-password" required/><button className="primary" disabled={busy}>{busy?'Signing in…':'Sign in'}</button>{error&&<p role="alert">{error}</p>}</form>;}
